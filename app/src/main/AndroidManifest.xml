<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".DocWalletApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.DocWallet.Splash">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.DocWallet.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/pdf" android:scheme="content" />
                <data android:mimeType="application/epub+zip" android:scheme="content" />
                <data android:mimeType="application/vnd.apple.pkpass" android:scheme="content" />
                <data android:mimeType="application/vnd.comicbook+zip" android:scheme="content" />
                <data android:mimeType="image/*" android:scheme="content" />
                <data android:mimeType="text/markdown" android:scheme="content" />
                <data android:mimeType="text/plain" android:scheme="content" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" android:scheme="content" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.viewer.EpubReaderActivity"
            android:exported="false"
            android:theme="@style/Theme.DocWallet" />
    </application>
</manifest>
